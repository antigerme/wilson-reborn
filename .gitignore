# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Original (copyrighted) game data — never commit
*.MAP
*.001
*.SCR
RESOURCE.*
SCRANTIC.*
data/
assets-original/
# Encrypted test bundle lives only under repos/ (password-protected); never at the
# repo root, where it was a redundant copy of repos/dist.zip.
/dist.zip

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/
